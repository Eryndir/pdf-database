import scalafx.Includes._
import scalafx.Includes.jfxControl2sfx
import scalafx.scene.layout.BorderPane
import scalafx.scene.control.Label

class SortingPane extends BorderPane {
  minWidth = (110 * 5) + 5
  maxWidth = (110 * 5) + 5
  children = new Label("leftView")
}
